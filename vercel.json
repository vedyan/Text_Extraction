{
    "builds": [{
        "src": "app/text_extraction_api.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "app/text_extraction_api.py"
        }
    ]
}